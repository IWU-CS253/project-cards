CREATE TABLE cards (
    card_id INT PRIMARY KEY,
    name VARCHAR(20),
    rank INT,
    type VARCHAR(5),
    pack VARCHAR(10),
    amount INT
);

CREATE TABLE collection (

);

CREATE TABLE store (

);